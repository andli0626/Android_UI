<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dream.myqiyi"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="4" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".AppParam" >
        <activity
            android:label="@string/app_name"
            android:name=".ui.MainView" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.tab4.AccountActivity" />
        <activity android:name=".ui.tab2.ChannelView" />
        <activity android:name=".ui.tab3.SearchActivity" />
        <activity android:name=".ui.tab1.HomeView" />
        <activity android:name=".ui.tab5.MoreActivity" />
    </application>

</manifest>